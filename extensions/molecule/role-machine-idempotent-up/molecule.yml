---
# Common Molecule configuration is located at <project root>/.config/molecule/config.yml
provisioner:
  playbooks:
    prepare: ../role-machine-default/prepare.yml

scenario:
  name: role-machine-idempotent-up
  test_sequence:
    - dependency
    - destroy
    - syntax
    - create
    - prepare
    - converge
    - verify
    - cleanup
    - destroy
